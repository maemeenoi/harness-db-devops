<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
  xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
  http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

  <!-- V3__proc.sql -->
  <changeSet id="V3-create-function" author="redgate">
    <sql>
      <![CDATA[
      CREATE OR REPLACE FUNCTION sales.get_recent_feedback_by_region()
      RETURNS TABLE (
          customer_id INT,
          territory_id INT,
          rating INT,
          feedback_text TEXT,
          submitted_at TIMESTAMPTZ
      ) AS $$
      BEGIN
        RETURN QUERY
        SELECT
          c.customerid,
          c.territoryid,
          f.rating,
          f.feedback_text,
          f.submitted_at
        FROM sales.customer c
        JOIN sales.customer_feedback f ON c.customerid = f.customer_id
        WHERE f.submitted_at > NOW() - INTERVAL '30 days'
        ORDER BY f.submitted_at DESC
        LIMIT 10;
      END;
      $$ LANGUAGE plpgsql;
      ]]>
    </sql>
  </changeSet>

</databaseChangeLog>